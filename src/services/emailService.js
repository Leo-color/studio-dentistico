// Email Service - DISABILITATO TEMPORANEAMENTE
//
// Tutte le funzioni email sono salvate in: EMAIL_SERVICE_BACKUP.md
// Per riabilitarle, vedi le istruzioni nel file di backup
//
// Funzioni disponibili ma disabilitate:
// - sendConfirmationEmail()
// - sendAdminNotificationEmail()
// - sendAdminCodeEmail()
// - sendContactMessageEmail()
// - sendReminderEmail()

// Placeholder functions che non fanno nulla
export const sendConfirmationEmail = async () => {
  console.log('📧 Email disabilitata (vedi EMAIL_SERVICE_BACKUP.md)');
  return true;
};

export const sendAdminNotificationEmail = async () => {
  console.log('📧 Email disabilitata (vedi EMAIL_SERVICE_BACKUP.md)');
  return true;
};

export const sendAdminCodeEmail = async () => {
  console.log('📧 Email disabilitata (vedi EMAIL_SERVICE_BACKUP.md)');
  return true;
};

export const sendContactMessageEmail = async () => {
  console.log('📧 Email disabilitata (vedi EMAIL_SERVICE_BACKUP.md)');
  return true;
};

export const sendReminderEmail = async () => {
  console.log('📧 Email disabilitata (vedi EMAIL_SERVICE_BACKUP.md)');
  return true;
};
